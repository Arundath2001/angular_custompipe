import { ChangelinkDirective } from './changelink.directive';

describe('ChangelinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangelinkDirective();
    expect(directive).toBeTruthy();
  });
});
